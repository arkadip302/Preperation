#include <bits/stdc++.h>
using namespace std;
struct activity
{
    int start_time;
    int end_time;
};
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        struct activity arr[n];
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i].start_time;
        }
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i].end_time;
        }
        for (int i = 0; i < n; i++)
        {
            
        }
    }
}