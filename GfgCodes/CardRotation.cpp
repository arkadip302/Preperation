#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        deque<int> q;
        int x;
        for (int i = 0; i < n; i++)
        {
            cin >> x;
            q.push_back(x);
        }
        int rotate = 1;
        while (q.size() != 1)
        {
        }
    }
}