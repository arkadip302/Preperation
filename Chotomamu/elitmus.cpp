#include<bits/stdc++.h>
using namespace std;
int main(){
    string s,s1="",s2;
    getline(cin,s);
    for(int i=0;i<s.size();i++){
        if(((int)s[i]>=65 && (int)s[i]<=90) || ((int)s[i]>=97 && (int)s[i]<=124)){
            s1=s1+s[i];
        }
    }
    for(int i=0;i<s1.size();i++){
        if((int)s1[i]>){
            s2=s2+s1[i];
        }
        else if(i+1<n && (int)s1[i+1]){

        }
    }
    
}